export type DashboardSummary = {
  as_of_date: string
  timezone: string
  currency_code: string
  active_hostel_count: number
  todays_supply: string
  revenue: string
  revenue_month: string
  revenue_definition: string
  pending_payments: string
  pending_definition: string
}
